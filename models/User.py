class User:
    def __init__(self, user_id, username, email, auth_source):
        self.user_id = user_id
        self.username = username
        self.email = email
        self.auth_source = auth_source
